package com.red.gotrade.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.red.gotrade.model.GoodsModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GOODS_MODEL".
*/
public class GoodsModelDao extends AbstractDao<GoodsModel, Long> {

    public static final String TABLENAME = "GOODS_MODEL";

    /**
     * Properties of entity GoodsModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Goods_id = new Property(1, String.class, "goods_id", false, "GOODS_ID");
        public final static Property Goods_price = new Property(2, Double.class, "goods_price", false, "GOODS_PRICE");
        public final static Property Goods_intro = new Property(3, String.class, "goods_intro", false, "GOODS_INTRO");
        public final static Property Goods_name = new Property(4, String.class, "goods_name", false, "GOODS_NAME");
        public final static Property Goods_sort = new Property(5, int.class, "goods_sort", false, "GOODS_SORT");
    }


    public GoodsModelDao(DaoConfig config) {
        super(config);
    }
    
    public GoodsModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GOODS_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"GOODS_ID\" TEXT," + // 1: goods_id
                "\"GOODS_PRICE\" REAL," + // 2: goods_price
                "\"GOODS_INTRO\" TEXT," + // 3: goods_intro
                "\"GOODS_NAME\" TEXT," + // 4: goods_name
                "\"GOODS_SORT\" INTEGER NOT NULL );"); // 5: goods_sort
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GOODS_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GoodsModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String goods_id = entity.getGoods_id();
        if (goods_id != null) {
            stmt.bindString(2, goods_id);
        }
 
        Double goods_price = entity.getGoods_price();
        if (goods_price != null) {
            stmt.bindDouble(3, goods_price);
        }
 
        String goods_intro = entity.getGoods_intro();
        if (goods_intro != null) {
            stmt.bindString(4, goods_intro);
        }
 
        String goods_name = entity.getGoods_name();
        if (goods_name != null) {
            stmt.bindString(5, goods_name);
        }
        stmt.bindLong(6, entity.getGoods_sort());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GoodsModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String goods_id = entity.getGoods_id();
        if (goods_id != null) {
            stmt.bindString(2, goods_id);
        }
 
        Double goods_price = entity.getGoods_price();
        if (goods_price != null) {
            stmt.bindDouble(3, goods_price);
        }
 
        String goods_intro = entity.getGoods_intro();
        if (goods_intro != null) {
            stmt.bindString(4, goods_intro);
        }
 
        String goods_name = entity.getGoods_name();
        if (goods_name != null) {
            stmt.bindString(5, goods_name);
        }
        stmt.bindLong(6, entity.getGoods_sort());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public GoodsModel readEntity(Cursor cursor, int offset) {
        GoodsModel entity = new GoodsModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // goods_id
            cursor.isNull(offset + 2) ? null : cursor.getDouble(offset + 2), // goods_price
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // goods_intro
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // goods_name
            cursor.getInt(offset + 5) // goods_sort
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GoodsModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setGoods_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setGoods_price(cursor.isNull(offset + 2) ? null : cursor.getDouble(offset + 2));
        entity.setGoods_intro(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setGoods_name(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setGoods_sort(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(GoodsModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(GoodsModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(GoodsModel entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
